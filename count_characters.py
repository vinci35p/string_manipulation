# Input full name
user_name = str(input("Enter your full name: "))

# Count characters in inputted string
characters_count = len(user_name)

# Print counted characters
print(f"The number of characters on your full name is {characters_count}.")